{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "starter files",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "d3": "^3.5.15",
    "light-server": "^1.1.5"
  },
  "devDependencies": {},
  "scripts": {
    "start": "npm run compile && npm run server",
    "compile": "babel main.js --out-file compiled.js",
    "server": "light-server -q -s . -w 'main.js # npm run compile' -w 'main.css # # reloadcss' -w 'index.html'"
  },
  "author": "russell goldenberg",
  "license": "MIT"
}
